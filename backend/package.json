{
  "name": "it-inventory-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "migrate": "node migrations/runMigrations.js",
    "start": "npm run migrate && node server.js",
    "dev": "npm run migrate && nodemon server.js",
    "setup": "node scripts/setup.js",
    "cleanup:test-data": "node scripts/removeSystemTestData.js"
  }
}
